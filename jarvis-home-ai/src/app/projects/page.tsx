import { Card } from "@/components/Card";
import { PageHeader } from "@/components/PageHeader";
import { projects } from "@/data/mock";
import { statusTone } from "@/lib/utils";
export default function ProjectsPage(){return <div><PageHeader title="Projects" subtitle="Keep major life and business moves organized with progress, notes, and next actions."/><div className="grid gap-4 lg:grid-cols-2">{projects.map(p=><Card key={p.id} title={p.name}><p className="text-jarvis-muted">{p.description}</p><div className="my-4 flex flex-wrap gap-2"><span className={`rounded-full border px-3 py-1 text-xs ${statusTone(p.status)}`}>{p.status}</span><span className={`rounded-full border px-3 py-1 text-xs ${statusTone(p.priority)}`}>{p.priority}</span></div><div className="mb-4"><div className="flex justify-between text-xs text-jarvis-muted"><span>Progress</span><span>{p.progress}%</span></div><div className="mt-2 h-2 rounded-full bg-jarvis-line"><div className="h-2 rounded-full bg-jarvis-glow" style={{width:`${p.progress}%`}} /></div></div><p className="font-semibold">Next actions</p><ul className="mt-2 space-y-1 text-sm text-jarvis-muted">{p.nextActions.map(a=><li key={a}>• {a}</li>)}</ul><p className="mt-4 font-semibold">Notes</p><ul className="mt-2 space-y-1 text-sm text-jarvis-muted">{p.notes.map(n=><li key={n}>• {n}</li>)}</ul></Card>)}</div></div>}
